services:
  - type: web
    name: memories-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5001
      - key: MONGODB_URI
        fromDatabase:
          name: memories-db
          property: connectionString 